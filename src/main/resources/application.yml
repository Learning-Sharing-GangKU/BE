spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gangku?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: gangku_user
    password: gangku1234!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update    # 개발 초기: update / 테스트만 할 땐 create
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080